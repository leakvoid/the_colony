using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ComputerPlayerEngine : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    void Update()
    {
        // per time interval
        // go over all colonists to
        // 1. accumulate pressure for service buildings construction
        // 2. accumulate pressure for needed resources
        // run construction scheduler to build

        // assign jobs to idle colonists
    }
}
